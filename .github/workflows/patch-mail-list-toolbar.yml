name: Patch mail list toolbar

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Patch userscript
        run: python3 .github/patch-mail-list-toolbar.py
      - name: Check JavaScript syntax
        run: node --check mail-adygheya-redesign.user.js
      - name: Commit fix and cleanup
        shell: bash
        run: |
          rm -f .github/patch-mail-list-toolbar.py
          rm -f .github/workflows/patch-mail-list-toolbar.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Restore message list toolbar clicks"
          git push origin HEAD:main
