name: Patch attachment preview hover

on:
  push:
    paths:
      - '.github/workflows/patch-attachment-preview-hover.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch userscript
        run: python .github/scripts/patch-attachment-preview-hover.py
      - name: Check JavaScript syntax
        run: node --check mail-adygheya-redesign.user.js
      - name: Commit fix and cleanup
        run: |
          rm -f .github/scripts/patch-attachment-preview-hover.py .github/workflows/patch-attachment-preview-hover.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix attachment preview hover overlay" || exit 0
          git push
