name: Apply mail toolbar fix

on:
  push:
    branches:
      - fix/mail-toolbar-copy-email

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Probe runner
        run: echo workflow-ok
