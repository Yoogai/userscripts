name: Patch single scrollbar v3.1.33

on:
  push:
    paths:
      - '.github/workflows/patch-single-scrollbar-v333.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch userscript
        run: python .github/scripts/patch-single-scrollbar-v333.py
      - name: Check JavaScript syntax
        run: node --check mail-adygheya-redesign.user.js
      - name: Commit fix and cleanup
        run: |
          rm -f .github/scripts/patch-single-scrollbar-v333.py .github/workflows/patch-single-scrollbar-v333.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix duplicate message list scrollbar" || exit 0
          git push
