name: Apply mail redesign patch

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Patch userscript
        run: python3 .github/patch-mail-redesign.py
      - name: Check JavaScript syntax
        run: node --check mail-adygheya-redesign.user.js
      - name: Commit patch and cleanup
        shell: bash
        run: |
          rm -f .patch-trigger
          rm -f .github/patch-mail-redesign.py
          rm -f .github/workflows/patch-mail-redesign-pr.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix mail toolbar and add email copy action"
          git push origin HEAD:main
