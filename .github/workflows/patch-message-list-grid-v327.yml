name: Patch message list subject grid

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Patch userscript
        run: python3 .github/patch-message-list-grid-v327.py
      - name: Check JavaScript syntax
        run: node --check mail-adygheya-redesign.user.js
      - name: Commit fix and cleanup
        shell: bash
        run: |
          rm -f .github/patch-message-list-grid-v327.py
          rm -f .github/workflows/patch-message-list-grid-v327.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix subject-first message list layout"
          git push origin HEAD:main
