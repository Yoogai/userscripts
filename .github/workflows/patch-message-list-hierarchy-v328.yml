name: Patch message list hierarchy

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Patch userscript
        run: python3 .github/patch-message-list-hierarchy-v328.py
      - name: Check JavaScript syntax
        run: node --check mail-adygheya-redesign.user.js
      - name: Commit fix and cleanup
        shell: bash
        run: |
          rm -f .github/patch-message-list-hierarchy-v328.py
          rm -f .github/workflows/patch-message-list-hierarchy-v328.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Refine subject-first message list hierarchy"
          git push origin HEAD:main
