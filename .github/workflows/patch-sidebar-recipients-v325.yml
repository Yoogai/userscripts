name: Patch sidebar and recipient regressions

on:
  push:
    paths:
      - '.github/workflows/patch-sidebar-recipients-v325.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch userscript
        run: python .github/scripts/patch-sidebar-recipients-v325.py
      - name: Check JavaScript syntax
        run: node --check mail-adygheya-redesign.user.js
      - name: Commit fix and cleanup
        run: |
          rm -f .github/scripts/patch-sidebar-recipients-v325.py .github/workflows/patch-sidebar-recipients-v325.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix collapsed folders and dynamic recipients" || exit 0
          git push
